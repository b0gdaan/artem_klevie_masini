# Raw data

- `smoke/`: синтетический снимок (в git), см. `SNAPSHOT.yaml` и `ground_truth.json`.
- Реальные снимки (`site_YYYY-MM-DD/`) хранятся только локально. Как их получить и запечатать, описано в `docs/DATA.md`.

Снимки неизменяемы: вместо правки создаётся новая папка и новый `SHA256SUMS`.
